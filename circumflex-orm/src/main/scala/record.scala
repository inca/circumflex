package ru.circumflex.orm

import org.slf4j.LoggerFactory
import ORM._

/* ## Record */

/**
 * *Record* is a cornerstone of relational model. In general, each instance of
 * pesistent class is stored as a single record in a table corresponding to that class.
 * Since Circumflex ORM employs the Active Record design approach to persistence,
 * each persistent class should subclass `Record`.
 */
class Record {

  

}